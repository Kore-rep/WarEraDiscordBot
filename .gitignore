# Dependencies
node_modules/

# Build output
dist/

# Environment variables
.env
.env.local
.env.*.local

# Server configuration (contains sensitive IDs)
config/serverConfig.json

# Legacy server configuration (for backwards compatibility)
servers.json

# SQLite database (generated at runtime; schema lives in prisma/migrations, which IS tracked)
data/*.db
data/*.db-journal
data/*.db-shm
data/*.db-wal

# Legacy JSON/CSV persistence (migrated into SQLite; kept out of git)
battles.json
data/weekly-damage/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# TypeScript
*.tsbuildinfo

